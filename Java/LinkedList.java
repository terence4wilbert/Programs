class LinkedList {
    Node head; // head of LinkedList

    /* Linked List Node */
    class Node {
        int data;
        Node next;

        // Constructor to reate a new node
        // next is by default initialized 
        // as null
        Node(int d) {
            data = d;
        }
    }
}